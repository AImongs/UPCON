# UPCON 런타임 의존성 (앱 실행에 반드시 필요한 것만)
# 개발/테스트 전용 패키지는 requirements-dev.txt 참고.
#
# 상한을 두는 이유: PySide6 는 Qt 메이저 업그레이드, keyring 은 백엔드 API,
# httpx 는 1.0 이전이라 마이너 단위로 깨질 수 있다.

PySide6>=6.11,<7          # UI (Qt6)
keyring>=25,<26           # API Key 저장 (Windows 자격 증명 관리자)
httpx>=0.27,<1.0          # fal.ai Platform API 호출 / 결과 다운로드
fal-client>=1.0,<2        # fal.ai Queue API (주의: fal_client.client 의 내부 심볼을 사용한다.
                          #   QUEUE_URL_FORMAT, SyncRequestHandle, _raise_for_status —
                          #   tests/test_cloud.py::test_fal_client_internal_api_contract 가 존재를 검증)
nvidia-ml-py>=12          # NVIDIA GPU/VRAM 감지 (pynvml). 없어도 앱은 뜨지만 GPU 감지가 제한됨
